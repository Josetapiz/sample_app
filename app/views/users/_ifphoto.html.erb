<a href="#myModal" data-toggle="modal"><%= image_tag( @user.photo.url(:small), alt: @user.name, class: "photo") %></a>
<div id="myModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel"><%= @user.name %></h3>
  </div>
  <div class="modal-body" style="padding: 1em 4em 1em 4em;">
    <p><%= image_tag( @user.photo.url(:large), alt: @user.name )%></p>
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
  </div>
</div>